#include <stdio.h>
int main () {
    int n, i;
    printf(" enter the number upto which numbers are to be printed:  ");
    scanf("%d", &n);
    
    printf(" numbers from 1 to %d are: \n",n);
    for ( i=1; i<=n; i++)
    printf( "%d \n",i );
    return 0;
}    
