#include <stdio.h>

int main() {
    int num;

    printf("Enter the value of n: ");
    scanf("%d", &num);

    int sum = num * num;

    printf("The sum of the first %d odd numbers is: %d\n", num, sum);

    return 0;
}
