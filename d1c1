#include <stdio.h> 
int main (){
    int a, b, sum;
    printf( "enter first num: " );
scanf("%d", &a );
printf (" enter second num; ");
scanf("%d", &b );
sum= a + b ;
printf("sum of the numbers entered is %d", sum );
return 0;
} 
