#include <stdio.h> 
int main (){ 
    float celcius, fahrenheit;
    printf( " enter temperature in degree celcius: "); 
     scanf("%f", &celcius);
     fahrenheit = celcius * 1.8 + 32; 
     printf( "%f degree celcius is equal to %.2f degree fahrenheit", celcius, fahrenheit);
     return 0; 
}
