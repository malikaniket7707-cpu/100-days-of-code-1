#include <stdio.h>
int main ()
{ 
    int n, sum ;
    printf( " enter the number upto which sum is to found:  ");
    scanf("%d", &n);
    sum= n * (n+1)/2;
    printf(" sum of n natural numbers is %d", sum );
    return 0;
}
