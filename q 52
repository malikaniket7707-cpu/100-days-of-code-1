#include <stdio.h>

int main() {
    int blocks[] = {1, 3, 5, 3, 1};
    int n = 5; // number of blocks

    for (int i = 0; i < n; i++) {
        for (int j = 1; j <= blocks[i]; j++) {
            printf("*\n");
        }
        printf("\n"); // blank line between blocks
    }

    return 0;
}
